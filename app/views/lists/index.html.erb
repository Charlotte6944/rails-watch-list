<div class="banner">
  <div class="container text-center text-middle">
    <h1>Your lists of movies!</h1>
  </div>
</div>

<div class="card-flex">
<% @lists.each do |list| %>
  <div class=" container card-home" style="width: 18rem;">
  <div class="card-name">
    <%= link_to list.name, list_path(list) %>
  </div>
  </div>
<% end %>
</div>
